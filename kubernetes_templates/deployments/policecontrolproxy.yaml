apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ namespace }}
  name: civilla-policecontrolproxy
  labels:
    run: civilla-policecontrolproxy
spec:
  replicas: 1
  selector:
    matchLabels:
      run: civilla-policecontrolproxy
  template:
    metadata:
      labels:
        run: civilla-policecontrolproxy
    spec:
      containers:
      - name: civilla-policecontrolproxy
        image: {{ civilla_policecontrolproxy_image }}
        volumeMounts:
          - name: bot-credentials
            mountPath: "/project/kubernetes"
            readOnly: true
          - name: services-urls
            mountPath: "/project/kubernetes"
      volumes:
        - name: bot-credentials
          secret:
            secretName: bot-credentials
        - name: services-urls
          secret:
            secretName: bot-credentials


